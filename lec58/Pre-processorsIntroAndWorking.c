/*
 * pre-processors is nothing but a text substitution tool.
 * they came in action into the first compilation process, the pre-processing phase, but they are not the part of the actual compiler.
 * All pre-processor cammands with sharp (#) symbol
 * some example of pre-processors commands:-
 * #include
 *
 * #define
 *
 * #undef
 *
 * #ifdef
 *
 * #ifndef
 *
 * #if
 *
 * #else
 *
 * #elif
 */
#include <stdio.h>

int main()
{
    
    return 0;
}

