/*
 * You have tocreate a command line utility to add/subtract/divide/multiply two numbers
 * the first command line argument must be the operation
 * the next arguments are being the two numbers. for example:-
 * >> command.c add 45 4
 * >> 49
 */
#include <stdio.h>

int main()
{
    
    return 0;
}

