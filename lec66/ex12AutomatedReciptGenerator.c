/*
 * you have to fill values in a template letter.txt
 * the letter.txt looks like:
 * thanks {{name}} for purchasing {{item}} from our outlet {{outlet}}
 * please visit our {{outlet}} again. we plan to ser e you again
 *
 * you have to read these variables from file and replace those values
 * {{name}} - Harry
 * {{item}} - Table fan
 * {{outlet}} - fan outlet
 */
#include <stdio.h>

int main()
{
    
    return 0;
}

