/* a c programme is made of constants, identifiers, string literals, keywords and symbols.
 * all these c programme building blocks is called tokens means token are nothing a constants or identifiers or keywords so on.
 *
 *keywords are notning but reserved words for c lanhuage. 32 keywords is in c language. you can not create constants, identifiers or variables by these keywords name.
 * identifiers are nothing but a valid name for a variable or function or constants
 *
 * for identifier we can use A-Z, a-z, 0-9, _
 *
 *c language is a case sensitive lanhuage.

 * you can use as many space among the tokens as you want but at least one space is needed to differenciate the individual tokens by the compiler

 * a semi-colon is used as a single statement tetminater
 */

#include <stdio.h>

int main()
{
    printf("Hello World");
    return 0;
}
