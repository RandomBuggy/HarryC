/*
 * suppose, ABC Pvt Ltd is a company who manages employee record of other company
 *
 * the employee id and name can be any integers and characters
 *
 * for 3 employee, you have to take employee id as input and display in the user screen using dynamic characters array
 * create only one dynamic character array, but before that you have to take employee id length as input in a integer variable
 *
 */
#include <stdio.h>

int main()
{
    
    return 0;
}

